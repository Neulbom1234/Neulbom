/** @type {import('next').NextConfig} */
const nextConfig = {
    // async rewrites() {
    //     return [
    //         {
    //             // source: 'images/:slug',
    //             source: '/upload/:slug',

    //             destination: 'http://3.35.146.40:8080/upload/:slug',
    //         }
    //     ]
    // }
};

export default nextConfig;
